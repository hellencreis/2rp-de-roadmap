#Exportar dados da tabela pokemon_table para outra tabela (new_table) em outro database (new_db)

USE new_db;

CREATE TABLE new_pokemon_table
AS
SELECT idnum, name, hp, speed FROM work_dataeng.pokemon_hellen;
